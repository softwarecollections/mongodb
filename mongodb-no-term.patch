diff -Nru mongodb-src-r2.0.6.orig/SConstruct mongodb-src-r2.0.6/SConstruct
--- mongodb-src-r2.0.6.orig/SConstruct	2012-06-04 09:42:54.000000000 -0400
+++ mongodb-src-r2.0.6/SConstruct	2012-08-01 16:12:34.280546684 -0400
@@ -713,11 +713,10 @@
     env.Append( LIBS=[] )
 
     #make scons colorgcc friendly
-    for key in ('HOME', 'TERM'):
-        try:
-            env['ENV'][key] = os.environ[key]
-        except KeyError:
-            pass
+    try:
+        env['ENV']['HOME'] = os.environ['HOME']
+    except KeyError:
+        pass
 
     if linux and has_option( "sharedclient" ):
         env.Append( LINKFLAGS=" -Wl,--as-needed -Wl,-zdefs " )
